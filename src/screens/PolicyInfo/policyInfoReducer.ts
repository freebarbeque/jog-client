import TabNavigation from './components/TabNavigation';

const initialState = {
    
};

export default (state = initialState, action:any={}) => {
    
    switch (action.type) {
        default:
            return state
    }
};